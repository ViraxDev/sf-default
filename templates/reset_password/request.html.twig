{% extends 'security/base.html.twig' %}

{% block title %}{{ parent() }}Réinitialiser votre mot de passe{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header bg-circle-shape bg-shape text-center p-2"><a class="font-sans-serif fw-bolder fs-4 z-1 position-relative link-light" href="../../../index.html" data-bs-theme="light">falcon</a></div>
        <div class="card-body p-4">
            <div class="text-center">
                <h4 class="mb-0"> Mot de passe oublié ?</h4><small>Entrez votre email et nous vous enverrons un lien de réinitialisation.</small>

                {{ form_start(requestForm, {'attr': {'class': 'mb-3 mt-4'}}) }}

                {{ form_widget(requestForm.email, {'attr': {'class': 'form-control', 'placeholder': 'Adresse email'}}) }}

                <div class="mb-3"></div>
                <button class="btn btn-primary d-block w-100 mt-3" type="submit" name="submit">Envoyer le lien de réinitialisation</button>

                {{ form_rest(requestForm) }}
                {{ form_end(requestForm) }}

                <a class="fs--1 text-600" href="#!">Je n'ai pas reçu de mail<span class="d-inline-block ms-1">&rarr;</span></a>
            </div>
        </div>
    </div>
{% endblock %}
